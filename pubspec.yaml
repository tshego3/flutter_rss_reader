name: flutter_rss_reader
description: "Flutter RSS Reader."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.3.0
  xml: ^6.5.0
  intl: ^0.20.2
  shared_preferences: ^2.5.1
  url_launcher: ^6.3.1
  flutter_html: ^3.0.0-beta.2
  share_plus: ^10.1.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
